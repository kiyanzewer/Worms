using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//public class Environment : MonoBehaviour
//{
  //  public GameObject ground;
    //public bool isSpawned = false;
    // Start is called before the first frame update
    //void Start()
    //{

    //}

    // Update is called once per frame
    //void Update()
    //{
      //  int index = LoadSceneOnClick.returnIndex();
        //if (isSpawned == false && index == 3)
        //{
          //for( int i = 0; i < 2; i++ )
            //{
              //for ( int e = 0; e < 1; e++ )
                //{
                  //  Vector3 pos = new Vector3(i, e, 0);
                    //var tempGround = Instantiate(ground, pos, Camera.main.transform.rotation);
                //}
            //}
            //isSpawned = true;
        //}
    //}
//}
